// Skipped in public CI — provider URLs are private (MongoDB / private catalog).
// Run locally against private/providers-catalog.json if you need URL checks.
console.log("verify-original-urls: skipped (catalog is remote/private)");
process.exit(0);
